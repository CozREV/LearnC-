


double x = 3;
double y = 5;


// double a = Math.Pow(x, 2);   // a = 9 - Math.Pow() raises a number to the power of another number.
// double b = Math.Sqrt(x);     // b = 1.732 - Math.Sqrt() finds the square root of a number.
// double c = Math.Abs(x);      // "x = -3" = "c = 3" - Math.Abs() finds the absolute value of a number.
// double d = Math.Round(x);    // "x = 3.14" = "d = 3" - Math.Round() rounds a number to the nearest integer.
// double e = Math.Ceiling(x);  // "x = 3.14" = "e = 4" - Math.Ceiling() rounds a number up to the nearest integer.
// double f = Math.Floor(x);    // "x = 3.14" = "f = 3" - Math.Floor() rounds a number down to the nearest integer.
// double g = Math.Max(x, y);   // "x = 3" and "y = 5" = "g = 5" - Math.Max() returns the larger of two numbers.
// double h = Math.Min(x, y);   // "x = 3" and "y = 5" = "h = 3" - Math.Min() returns the smaller of two numbers.

Console.WriteLine();

Console.ReadKey();